name: ci
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - run: corepack enable
      - run: pnpm install --frozen-lockfile

      # codeform ships compiled JS; the self-spec and examples import it and use
      # its bin, so build it before anything downstream resolves `codeform`.
      - name: build
        run: pnpm -C ts build

      - name: typecheck + unit tests
        working-directory: ts
        run: |
          pnpm exec tsc --noEmit
          pnpm exec tsx --test src/*.test.ts

      # The self-spec now round-trips: `gen` produces a decisions file that
      # compiles, so `check` proves the mirror conforms rather than only that the
      # spec parses. Regenerating first catches drift between the two.
      - name: self-spec round-trips and conforms
        working-directory: spec
        run: |
          ./node_modules/.bin/codeform gen spec.ts --overwrite
          git diff --exit-code decisions.gen.ts
          ./node_modules/.bin/codeform check spec.ts
          ./node_modules/.bin/codeform emit spec.ts > /dev/null

      - name: examples conform
        run: |
          for d in account hangman ts; do
            (cd "examples/$d" && ../node_modules/.bin/codeform check spec.ts)
          done
